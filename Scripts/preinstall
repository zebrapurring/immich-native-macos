#!/bin/sh

set -eu

PASSWORD=$(date +%s)
echo "export PASSWORD=\"$PASSWORD\"" >> "./config.sh"

# shellcheck disable=SC1091
. "./config.sh"

install_logfile="/tmp/immich-install.log"
sh "./createlog.sh" "$install_logfile"
{
    sh "./installdependencies.sh"
    sh "./configurepostgres.sh" "$PASSWORD"
} >> "$install_logfile" 2>&1
