#!/bin/sh

set -eu

# shellcheck disable=SC1091
. "./config.sh"

install_logfile="/tmp/immich-install.log"
{
    sh "./createuser.sh"
    sh "./createpaths.sh"
    sh "./build.sh" "$PASSWORD"
    sh "./installdaemons.sh"
} 2>&1 | tee -a "$install_logfile"
